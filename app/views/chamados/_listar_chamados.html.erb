<% if @chamados_andamento.any? %>
  <ul class="listar_no_border">
    <!--<h1><span class="label label-default lab">Chamados em andamento</span></h1>-->
    <p><strong>Chamados em andamento: </strong></p>
    <% @chamados_andamento.each do |andamento| %>
      <li>
        <div class="panel panel-primary chamadoPanel">
          <div class="panel-heading caput">
            <h3 class="panel-title">
              <%= link_to andamento.descricao, andamento %>
              <%= link_to "Concluir chamado", edit_chamado_path(andamento, :concluir => "concluir", :status => "C"), class: "botao_chamado btn btn-primary" %>
            </h3>
          </div>
          <table class="table">
            <thead>
              <tr>
                <th>Subcategoria</th>
                <th>Setor</th>
                <th>Aberto em</th>
                <th>Prioridade</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><%= andamento.objeto.nome %></td>
                <td><%= andamento.solicitante.sector.nome %></td>
                <td><%= andamento.created_at.to_s.chomp(' -0300') %></td>
                <td><%= andamento.prioridade %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>
<br>
<% if @chamados_espera.any? %>
  <ul class="listar_no_border">
    <!--<h1><span class="label label-default lab">Chamados em espera</span></h1>-->
    <p><strong>Chamados em espera: </strong></p>
    <% @chamados_espera.each do |espera| %>
      <li>
        <div class="panel panel-info chamadoPanel">
          <div class="panel-heading caput">
            <h3 class="panel-title">
              <%= link_to espera.descricao, espera %>
              <%= link_to("Aceitar chamado", chamado_path(espera, :status => "A"), :method => :put, class: "botao_chamado_info btn btn-info") %>
            </h3>
          </div>
          <table class="table">
            <thead>
              <tr>
                <th>Subcategoria</th>
                <th>Setor</th>
                <th>Aberto em</th>
                <th>Prioridade</th>
              </tr>
            </thead>
            <tbody>
              <tr>
                <td><%= espera.objeto.nome %></td>
                <td><%= espera.solicitante.sector.nome %></td>
                <td><%= espera.created_at.to_s.chomp(' -0300') %></td>
                <td><%= espera.prioridade %></td>
              </tr>
            </tbody>
          </table>
        </div>
      </li>
    <% end %>
  </ul>
<% end %>