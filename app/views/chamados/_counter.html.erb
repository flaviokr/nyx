<div id="avisos">
<% if contadorAlerta > 0 %>
   	<div class="bola" id="aviso_estourados">
   		<%= contadorAlerta %>
   	</div>
<% else %>
<% end %>

<% if contadorNovos > 0 %>
   	<div class="bola" style="background: blue" id="aviso_novos">
   		<%= contadorNovos %>
   	</div>
<% else %>
<% end %> 
</div>